---
description: commit all changes with an auto-generated message and push to remote
---

// turbo-all

1. Stage all changes:
```
git add -A
```

2. Commit with an auto-generated message describing the changes:
```
git commit -m "<short imperative summary of what was changed>"
```

3. Push to the current remote branch:
```
git push
```
